/**
 * General content types in the system.
 */
export enum ContentTypes {
    JSON = 'application/json'
}

/**
 * Response types from the Laravel backend.
 */
export enum ResponseTypes {
    SUCCESS = 'success',
    ERROR = 'error'
}