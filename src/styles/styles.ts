import { StyleSheet } from "react-native";

/**
 * These are the styles that can be used within the application.
 */
const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        paddingHorizontal: 10,
        width: '100%'
    },
    homeCard: {
        width: '50%'
    }
});

export default styles;